name: photo_booth
description: A simplified photo booth application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^0.8.3+1
  cupertino_icons: ^1.0.0
  flutter_bloc: ^7.0.0
  get_it: ^6.0.0
  sqflite: ^2.0.0+4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true